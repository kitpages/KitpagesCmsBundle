<div class="kit-cms kit-cms-zone">
    <div class="kit-cms-zone-toolbar">
        <ul class="kit-cms-menu">
            <li>Zone</li>
            {% for action, url in listAction %}
            <li><a href='{{url}}' >{{action}}</a></li>
            {% endfor %}
        </ul>
        <div class="kit-cms-zone-container">
            {{ htmlBlock | raw}}
        </div>
     </div>
</div>

