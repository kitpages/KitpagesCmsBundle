<div class="kit-cms kit-cms-block-toolbar">
    <ul class="kit-cms-menu">
        <li>Block</li>
        {% for action in actionList %}
            <li class="{{action.class|default()}}" ><a href="{{action.url}}">{{action.label}}</a></li>
        {% endfor %}
    </ul>
</div>

